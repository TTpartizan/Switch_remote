# Система управления сетевыми коммутаторами

## 🌐 Обзор проекта

Веб-приложение для централизованного управления сетевыми коммутаторами с функционалом выполнения команд, администрирования пользователей и мониторинга сетевого оборудования.

## ✨ Основные возможности

- Аутентификация и авторизация пользователей
- Ролевая модель (администратор/пользователь)
- Управление коммутаторами
- Выполнение команд на коммутаторах
- Гибкая настройка подключений

## 🏗️ Архитектура проекта

### Структура директорий
```
network_switch_management/
│
├── main.py                 # Основной файл приложения
├── database.py             # Модели базы данных
├── security.py             # Утилиты безопасности
│
├── dependencies/           # Зависимости и утилиты
│   ├── db.py               # Управление сессиями базы данных
│   └── auth.py             # Авторизация и проверка прав
│
├── services/               # Бизнес-логика
│   ├── user_service.py     # Сервис управления пользователями
│   ├── switch_service.py   # Сервис управления коммутаторами
│   ├── command_service.py  # Сервис управления командами
│   └── network_service.py  # Сервис работы с сетью
│
├── templates/              # HTML-шаблоны
│   ├── index.html          # Главная страница
│   └── login.html          # Страница входа
│
└── static/                 # Статические файлы
    └── styles.css          # Стили приложения
```

## 📦 Сущности базы данных

### 1. Пользователи (`User`)
- `id`: Уникальный идентификатор
- `username`: Имя пользователя
- `hashed_password`: Хэшированный пароль
- `is_admin`: Флаг администратора

### 2. Коммутаторы (`Switch`)
- `id`: Уникальный идентификатор
- `ip_address`: IP-адрес коммутатора
- `hostname`: Имя коммутатора
- `brand`: Производитель (например, Cisco)

### 3. Команды (`Command`)
- `id`: Уникальный идентификатор
- `name`: Название команды
- `template`: Шаблон команды

## 🔐 Механизм аутентификации

1. Использует JWT (JSON Web Token)
2. Хеширование паролей с помощью bcrypt
3. Куки-based аутентификация
4. Раздельные права для администраторов и пользователей

## 🛠️ Основные сервисы

### UserService
- Создание пользователей
- Обновление данных пользователей
- Удаление пользователей
- Листинг пользователей

### SwitchService
- Добавление коммутаторов
- Обновление информации о коммутаторах
- Удаление коммутаторов
- Листинг коммутаторов

### CommandService
- Создание команд
- Обновление команд
- Удаление команд
- Листинг команд

### NetworkService
- Тестирование подключения к коммутаторам
- Выполнение команд на коммутаторах

## 🚀 Эндпоинты API

### Аутентификация
- `POST /token`: Получение токена
- `GET /logout`: Выход из системы

### Администрирование
- CRUD операции для пользователей `/admin/users/`
- CRUD операции для коммутаторов `/admin/switches/`
- CRUD операции для команд `/admin/commands/`
- Тестирование подключения `/admin/test-switch-connection`

## 🔧 Настройка и установка

1. Клонировать репозиторий
2. Установить зависимости:
   ```bash
   python -m pip install -r requirements.txt
   ```

3. Настроить `.env` файл

4. Инициализировать базу данных:
   ```bash
   python init_db.py
   ```

5. Запустить приложение:
   ```bash
   python main.py
   ```

## 🔑 Тестовые учетные записи

- Администратор:
  - Логин: `admin`
  - Пароль: `admin_password`

- Пользователь:
  - Логин: `user`
  - Пароль: `user_password`

## 🔧 Поддерживаемые типы устройств

В текущей версии используется тип устройства `cisco_ios`. 

### Список поддерживаемых типов устройств Netmiko:

- `cisco_ios`
- `cisco_xe`
- `cisco_xr`
- `juniper_junos`
- `aruba_os`
- `hp_procurve`
- `dell_force10`
- `huawei`

Для использования другого типа устройства измените значение `SWITCH_DEVICE_TYPE` в `.env` файле.

### Примечание

Убедитесь, что выбранный тип устройства соответствует вашему оборудованию и поддерживается библиотекой Netmiko.

## 🛡️ Безопасность

- Хеширование паролей
- Защита эндпоинтов
- Ролевая модель доступа
- Использование env-переменных для конфигурации

## 📋 Требования

- Python 3.8+
- FastAPI
- SQLAlchemy
- Netmiko
- Uvicorn

## 🔮 Планы развития

- Логирование действий
- Расширенная статистика
- Поддержка большего количества типов коммутаторов
- Мониторинг состояния устройств

## 🤝 Contributing

1. Форкните репозиторий
2. Создайте свою ветку (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Распространяется под [указать лицензию]

## 📞 Контакты

[Ваши контактные данные]
